import { Stack } from "expo-router";

/** Pilha da aba Campanhas: disparos do dia e o formulário de campanha. */
export default function CampaignsLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
