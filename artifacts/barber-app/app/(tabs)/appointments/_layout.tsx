import { Stack } from "expo-router";

/** Pilha da aba Atendimentos: lista e formulário de novo atendimento. */
export default function AppointmentsLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
