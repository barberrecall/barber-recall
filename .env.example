# Copie para `.env` e preencha. O `.env` real fica fora do Git.
#
# Carregado por:
#   - artifacts/api-server (via `node --env-file-if-exists`)
#   - lib/db/drizzle.config.ts (via `process.loadEnvFile`)

# Postgres. Em desenvolvimento costuma ser um banco Neon.
DATABASE_URL=postgresql://usuario:senha@host/banco?sslmode=require

# Host do banco de DESENVOLVIMENTO — no Neon, o endpoint do branch `dev`.
#
# Comandos que alteram dados ou schema (`db push`, `check:paywall`) comparam o
# host do DATABASE_URL com este valor e se recusam a rodar quando os dois
# divergem. Sem esta variável eles não rodam: a trava recusa por padrão em vez
# de liberar por omissão (ver lib/db/src/guard.ts).
#
# Só o host, sem usuário, senha ou caminho.
DEV_DB_HOST=ep-seu-branch-dev.us-east-2.aws.neon.tech

# Porta do servidor de API. O app móvel assume 8080 ao derivar o endereço
# da API a partir do host do Metro (artifacts/barber-app/src/lib/env.ts).
PORT=8080

NODE_ENV=development

# Assina os cookies de sessão da web E os tokens Bearer do app.
# Trocar este valor desloga todo mundo nas duas superfícies.
# Em produção é obrigatório: o servidor se recusa a subir sem ele.
SESSION_SECRET=troque-por-um-valor-longo-e-aleatorio

# Opcionais — cada um desliga uma funcionalidade se ausente.

# Checkout do Mercado Pago. Sem ele as rotas de pagamento falham.
# MERCADOPAGO_ACCESS_TOKEN=

# Segredo da assinatura do webhook, no mesmo painel do Mercado Pago em que o
# webhook é cadastrado. Sem ele o webhook recusa TODA notificação — inclusive
# as legítimas —, e um pagamento aprovado nunca libera o acesso.
#
# A recusa é proposital: o endereço do webhook é público e o corpo carrega só um
# id de recurso, então aceitar sem verificar deixaria qualquer um conceder
# 30 dias de plano a si mesmo. O servidor avisa no start quando falta.
# MERCADOPAGO_WEBHOOK_SECRET=

# Domínio público usado no retorno do pagamento. Sem ele o callback
# aponta para um endereço inválido fora do Replit.
# APP_URL=

# Painel de super admin. Sem os dois, /admin/login responde 503.
# ADMIN_EMAIL=
# ADMIN_PASSWORD=
