{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "seed:demo": "tsx --env-file-if-exists=../.env ./src/seed-demo.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "icons": "tsx ./src/make-icons.ts",
    "check:paywall": "tsx --env-file-if-exists=../.env ./src/check-paywall.ts",
    "check:webhook": "tsx --env-file-if-exists=../.env ./src/check-webhook.ts"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:"
  }
}
